# -----------------------------------------
# paku-digest – Example Environment File
# Copy this file to `.env` and adjust values
# -----------------------------------------

# Execution environment profile
# local | dev | prod
PAKU_ENV=local

# Logging level for internal logger
# DEBUG | INFO | WARNING | ERROR
PAKU_LOG_LEVEL=INFO

# Default OCR engine used by paku-digest
# Available: stub
# Optional (only if dependencies/config present):
#   paddle        – uses local PaddleOCR installation
#   chandra-api   – uses an OpenAI-compatible API (placeholder for now)
PAKU_DEFAULT_OCR=stub

# Working directory used for caches, exports, temp files, etc.
# Use "." to place everything in the project root.
# Or create a dedicated folder: ./data  ./work
PAKU_WORKDIR=.

# -----------------------------------------------------
# PaddleOCR settings (optional)
# Only used if PaddleOCREngine is available.
# -----------------------------------------------------

# Default language for PaddleOCR
# Examples: en, it, fr, ch, jp
PAKU_PADDLE_LANG=en


# -----------------------------------------------------
# Chandra OCR via API (placeholder)
# Leave empty unless testing an OpenAI-compatible API.
# -----------------------------------------------------

# Base URL of the API endpoint (OpenAI-like /v1/)
# Example:
#   http://localhost:8000/v1/
#   https://api.example.com/v1/
PAKU_CHANDRA_API_URL=

# API key for authentication if required
PAKU_CHANDRA_API_KEY=
